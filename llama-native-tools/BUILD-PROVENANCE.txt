================================================================================
llama-native-tools/BUILD-PROVENANCE.txt
what was actually built, when, by what, and with what result
================================================================================

WHY THIS FILE EXISTS
--------------------------------------------------------------------------------
A binary in a package is anonymous. This file is how anyone later can tell which
native library came from which source, which toolchain and which build machine -
without which "rebuild it and see if it matches" is not a question that can even
be asked. Every time a binary is adopted into the package's runtimes/ tree, its
section here is updated from that build's output/<rid>/BUILD-INFO.txt.

Common to every entry below
--------------------------------------------------------------------------------
  Source          : llama-native-tools/llama.cpp/ - vendored upstream subset
                    (CMakeLists.txt, LICENSE, cmake/, ggml/, src/, include/)
  llama.cpp       : tag b10221, commit 815a2a5915f22ce6a760c676389c5dfe8535c08f
                    (2026-08-01), ggml 0.18.0
  Licence         : MIT (llama.cpp/LICENSE, shipped as LICENSE-LlamaCpp.txt beside
                    every binary; see ../THIRD-PARTY-NOTICES.txt)
  Patches applied : NONE. The vendored source is unmodified upstream.
  Wrapper         : llama-native-tools/wrapper/CMakeLists.txt - static archives
                    linked whole-archive into ONE shared library named
                    codebrix_llama; exports restricted to llama_* / ggml_* /
                    gguf_* / codebrix_llama_*; Release with debug info, then
                    stripped for shipping
  cmake options   : -DCMAKE_BUILD_TYPE=Release -DBUILD_SHARED_LIBS=OFF
                    -DCMAKE_POSITION_INDEPENDENT_CODE=ON -DGGML_NATIVE=OFF
                    -DGGML_OPENMP=OFF -DGGML_CPU_KLEIDIAI=OFF -DGGML_BACKEND_DL=OFF
                    -DGGML_AVX512=OFF -DLLAMA_OPENSSL=OFF -DLLAMA_CURL=OFF
                    -DLLAMA_BUILD_COMMON=OFF -DLLAMA_BUILD_TOOLS=OFF
                    -DLLAMA_BUILD_EXAMPLES=OFF -DLLAMA_BUILD_TESTS=OFF
                    -DLLAMA_BUILD_APP=OFF -DLLAMA_BUILD_SERVER=OFF
                    (plus the per-architecture options in linux/pins.env,
                    repeated under each RID below)
  Conformance     : test-vectors/codebrix-conformance-tiny.gguf (sha256
                    66c90c32aea69570911279c49ca1252e60408a2fe2218000cff54444cdb6e7d9)
                    run through the built library; every logit within 0.001 of
                    test-vectors/EXPECTED.txt, greedy argmax exact, no NaN. The
                    reference was established on the osx-x64 CPU path on
                    2026-09-15 (see test-vectors/README.txt), so for every OTHER
                    RID the conformance pass is a cross-architecture check
                    against an AVX2-generated reference.
  Gate            : architecture, all LLAMA_API exports (233) + extras (15),
                    export surface exactly the four prefixes, system-only
                    dependencies, platform floor (glibc / minos / static CRT),
                    signature (macOS), dlopen smoke test (75 entry points +
                    backend init + device enumeration), model regeneration
                    byte-identical to the committed asset, conformance. On
                    macOS, additionally at COMPILE time (from 2026-09-15):
                    -Werror=unguarded-availability-new, so no API newer than
                    the floor can be called unguarded - see macos/README.txt,
                    THE MINIMUM macOS VERSION.


================================================================================
osx-x64                                                          BUILT + PASSED
================================================================================
Built            : 2026-09-16 04:07:48 UTC (2026-09-15 local) - the REBUILD at
                   the 13.3 floor. It replaces the entry for the 11.0-floor build
                   of 2026-09-16 00:05:52 UTC (sha256 b06df3a7..., LC_UUID
                   5830F46C-EFA7-3F60-840C-45833A3F0831), which was adopted the
                   same day and withdrawn when the osx-arm64 run (below) showed
                   that it weak-imported Accelerate's cblas_sgemm$NEWLAPACK$ILP64
                   (a 13.3+ symbol) and would have crashed at the first BLAS
                   matmul on macOS 11.0-13.2. Everything that build established
                   about the slice (reproducibility, the Metal NaN finding, the
                   export surface) still holds and is kept below.
Built by         : macos/build-osx-x64.sh (native route, Intel Mac)
Build host       : Intel Mac mini (2018), i7-8700B, macOS 15.8 (x86_64),
                   SDK 26.2
Duration         : 60 s (warm ccache; the first build of the day was about
                   4 minutes cold)
Network          : not used - the vendored source, the conformance model and
                   the expected logits are all files in this repository
Compiler         : Apple clang version 17.0.0 (clang-1700.6.4.2)
cmake / ninja    : 4.4.3 / 1.13.2 (both equal to the pins)
arch options     : -DGGML_METAL=OFF -DGGML_BLAS=ON -DGGML_BLAS_VENDOR=Apple
CPU baseline     : x86-64 with AVX2, FMA, F16C, BMI2 (no AVX-512)
GPU backend      : none. Metal OFF - a CPU-only slice by decision (Jeremy,
                   2026-09-15), and necessarily so: at this commit ggml-metal
                   returned NaN for every logit on the Intel UHD 630 (verified
                   with a separate Metal-enabled trial build the same day).
Deployment target: 13.3 (checked by the gate, not merely requested; and PROVEN
                   at compile time by -Werror=unguarded-availability-new - zero
                   warnings in the build log). `nm -m` on the shipped file shows
                   NO weak imports at all: cblas_sgemm$NEWLAPACK$ILP64 is a
                   normal Accelerate import, as it must be at 13.3.

File             : libcodebrix_llama.dylib   install name @rpath/libcodebrix_llama.dylib
Size (stripped)  : 4,198,752 bytes (as shipped, signed; measured after codesign)
Size (unstripped): 5,195,632 bytes
SHA256           : 03af0e77f6cb2fb6207d53688c796baf0d8bd9d237c7264a6e55a5bd8c5a4b76
                   *** REPRODUCIBLE EXCEPT FOR LC_UUID - see Reproducibility below.
                   A rebuild legitimately produces a different sha256. ***
SHA256 unstripped: 558ce454a4289ddb76b6e3e7e81a81d988e7faa7fca3364ecd0d34bd3467eb56
LC_UUID          : E98A82F7-4F50-3E3E-955F-96CC1242669A (NOT stable across builds;
                   the stored unstripped twin and dSYM carry this one - verified
                   equal on all three with dwarfdump --uuid at adoption)
Debug symbols    : libcodebrix_llama.dylib.dSYM, 57 MB, stored under unstripped/osx-x64/
Dependencies     : /usr/lib/libSystem.B.dylib
                   /System/Library/Frameworks/Accelerate.framework/Versions/A/Accelerate
                   /usr/lib/libc++.1.dylib
Weak imports     : none
Signature        : ad-hoc (codesign --sign -), applied after strip and
                   install_name_tool; re-verified after the copy into runtimes/
Exports          : 1,159 symbols, all llama_* / ggml_* / gguf_* / codebrix_llama_*;
                   all 233 LLAMA_API functions present
Reproducibility  : established on the 11.0-floor build of the same day - four
                   consecutive from-scratch runs (two with ZERO_AR_DATE=1, which
                   changed nothing). Consecutive UNSTRIPPED builds differ in 18
                   bytes (the 16-byte LC_UUID and 2 bytes in the symbol-table
                   region); the STRIPPED, signed files differ in 112 bytes (UUID
                   + the signature covering it). All code and data identical;
                   every run passed the whole gate. Cause and reasoning:
                   macos/README.txt, "WHAT HAS AND HAS NOT BEEN VERIFIED", item
                   1. Compare size, exports and gate result, not the hash. Not
                   re-measured over repeated runs at the 13.3 floor.
Gate             : architecture ok, 248/248 required exports, export surface
                   exact, install name ok, system-only dependencies, minos 13.3
                   ok, signature ok, smoke test passed (75/75 entry points,
                   2 devices: BLAS/Accelerate + CPU, system info "AVX = 1 |
                   AVX2 = 1 | F16C = 1 | FMA = 1 | BMI2 = 1 | LLAMAFILE = 1 |
                   ACCELERATE = 1 | REPACK = 1"), model regeneration
                   byte-identical, conformance CPU: 12 positions, 64 logits
                   each, max |diff| 4.98e-08, PASSED (identical spread to the
                   11.0-floor build: the floor changes which symbols are
                   imported, not the arithmetic)
Adopted into src/CodeBrix.Ollama.ModelRunner/runtimes/osx-x64/native/ : YES,
                   2026-09-15, with llama.cpp's LICENSE beside it as
                   LICENSE-LlamaCpp.txt. Unstripped twin + dSYM stored under
                   unstripped/osx-x64/ (SHA256SUMS regenerated) in the same
                   commit, replacing the 11.0-floor twin.


================================================================================
osx-arm64                                                          BUILT + PASSED
================================================================================
Built            : 2026-09-16 03:51:11 UTC (2026-09-15 local)
Built by         : macos/build-osx-arm64.sh (native build, Apple Silicon Mac)
Build host       : Apple Silicon Mac mini, M2 Pro (10 cores), macOS 27.0 (arm64),
                   Xcode 27.0
Duration         : 30 s cold (this run); 44 s on the first run the same day
Network          : not used - the vendored source, the conformance model and
                   the expected logits are all files in this repository
Compiler         : Apple clang version 21.0.0 (clang-2100.3.34.2)
cmake / ninja    : 4.4.3 / 1.13.2 (both equal to the pins)
arch options     : -DGGML_CPU_ARM_ARCH=armv8.2-a+dotprod+fp16 -DGGML_METAL=ON
                   -DGGML_METAL_EMBED_LIBRARY=ON -DGGML_BLAS=ON -DGGML_BLAS_VENDOR=Apple
CPU baseline     : armv8.2-a + dotprod + fp16
GPU backend      : Metal, shader library embedded in the dylib (no .metallib
                   ships beside it; compiled at first load - 9 s on the M2 Pro
                   the first time, 0.03 s afterwards from macOS's shader
                   cache). Verified by the gate's Metal conformance pass.
Deployment target: 13.3 (checked by the gate, not merely requested; and PROVEN
                   at compile time by -Werror=unguarded-availability-new - zero
                   warnings). This is the run that raised the floor from 11.0:
                   the first run, at 11.0, passed every gate check but its build
                   log showed cblas_sgemm$NEWLAPACK$ILP64 (13.3+) and
                   -[MTLSharedEvent waitUntilSignaledValue:timeoutMS:] (12.0+)
                   called unguarded, i.e. weak-imported. That run was NOT
                   adopted. Reasoning: macos/README.txt, THE MINIMUM macOS
                   VERSION; the pin: linux/pins.env MACOS_MIN_VERSION.

File             : libcodebrix_llama.dylib   install name @rpath/libcodebrix_llama.dylib
Size (stripped)  : 4,307,696 bytes (as shipped, signed)
Size (unstripped): 5,524,600 bytes
SHA256           : 65339b09a95f795b075b6e9ea61d2b38fc82b9753a4efd17863e5974c8ac47c4
                   *** Expected to be reproducible except for LC_UUID, as osx-x64
                   is; not yet measured over repeated runs on this slice. ***
SHA256 unstripped: 2a59d2717dca40aa9ad52eee19fa7d35278f9fe8bb99111882dc807c8e77e455
LC_UUID          : 88DBABCC-377F-3BBB-A943-F3E67AD56808 (NOT stable across builds;
                   the stored unstripped twin and dSYM carry this one)
Debug symbols    : libcodebrix_llama.dylib.dSYM, 57 MB, stored under unstripped/osx-arm64/
Dependencies     : /System/Library/Frameworks/Accelerate.framework/Versions/A/Accelerate
                   /usr/lib/libSystem.B.dylib
                   /System/Library/Frameworks/Foundation.framework/Versions/C/Foundation
                   /System/Library/Frameworks/Metal.framework/Versions/A/Metal
                   /System/Library/Frameworks/MetalKit.framework/Versions/A/MetalKit
                   /usr/lib/libc++.1.dylib
                   /System/Library/Frameworks/CoreFoundation.framework/Versions/A/CoreFoundation
                   /usr/lib/libobjc.A.dylib
Weak imports     : only _OBJC_CLASS_$_MTLResidencySetDescriptor (macOS 15 feature,
                   guarded by @available in ggml-metal) and libSystem's
                   __availability_version_check - both correct
Signature        : ad-hoc (codesign --sign -), applied after strip and
                   install_name_tool; re-verified after the copy into runtimes/
Exports          : 1,392 symbols, all llama_* / ggml_* / gguf_* / codebrix_llama_*;
                   all 233 LLAMA_API functions present
Gate             : architecture ok, 248/248 required exports, export surface
                   exact, install name ok, system-only dependencies (Metal
                   list), minos 13.3 ok, signature ok, smoke test passed (75/75
                   entry points, 3 devices: MTL0 Apple M2 Pro + BLAS/Accelerate
                   + CPU, system info "MTL : EMBED_LIBRARY = 1 | CPU : NEON = 1
                   | ARM_FMA = 1 | FP16_VA = 1 | DOTPROD = 1 | LLAMAFILE = 1 |
                   ACCELERATE = 1 | REPACK = 1"), model regeneration
                   byte-identical, conformance CPU: 12 positions, 64 logits
                   each, max |diff| 1.43e-04, PASSED; conformance Metal (all
                   layers offloaded): max |diff| 1.14e-04, PASSED. Both spreads
                   are the expected cross-architecture rounding against the
                   AVX2-generated reference (the x64 CPU path's 4.98e-08 was
                   AVX2 checking AVX2); argmax exact at every position.
Adopted into src/CodeBrix.Ollama.ModelRunner/runtimes/osx-arm64/native/ : YES,
                   2026-09-15, with llama.cpp's LICENSE beside it as
                   LICENSE-LlamaCpp.txt. Unstripped twin + dSYM stored under
                   unstripped/osx-arm64/ (SHA256SUMS extended) in the same commit.


================================================================================
win-x64                                                          BUILT + PASSED
================================================================================
Built            : 2026-09-16 06:25:25 UTC (2026-09-15 local) - the THIRD run
                   of the day; the first two are under First-run fixes below.
                   The first run built a working DLL (its smoke test, model
                   regeneration and conformance all passed) but the gate's
                   static checks were wrong in two ways and the export surface
                   was wrong in one; this run is the first at which every
                   check ran, was believed, and passed.
Built by         : windows\build-win-x64.ps1 (native, MSVC)
Build host       : Windows 11 Pro (10.0.26200, AMD64), 12th Gen Intel Core
                   i7-12850HX; Visual Studio Professional 2026 18.10
                   (MSVC toolset 14.51.36231, Windows SDK 10.0.26100.0)
Duration         : 44 s (cold: the script deletes the build tree every run)
Network          : not used - the vendored source, the conformance model and
                   the expected logits are all files in this repository
Compiler         : Microsoft (R) C/C++ Optimizing Compiler Version 19.51.36257
                   for x64; link.exe 14.51
cmake / ninja    : 4.4.3 / 1.13.2 (both equal to the pins; installed with
                   `pip install --user cmake==4.4.3 ninja==1.13.2` - the
                   Visual Studio-bundled cmake on this machine is 4.3.1, below
                   the pin, and vcvarsall did not put it ahead on PATH)
arch options     : (none) - the x64 baseline is what GGML_NATIVE=OFF gives;
                   on MSVC that is /arch:AVX2 plus ggml's own GGML_AVX2
                   GGML_FMA GGML_F16C __BMI2__ definitions
CPU baseline     : x86-64 with AVX2, FMA, F16C, BMI2 (no AVX-512)
GPU backend      : none (CPU-only slice)
CRT              : static (MultiThreaded, -MT on every object)
Patches applied  : none

File             : codebrix_llama.dll
Size             : 7,017,984 bytes (a DLL carries no debug info itself; there
                   is no strip step on Windows - the .pdb is the twin)
SHA256           : 9f70999fcfc5bc8f835af59b6adf74c027c5c2b97db215bcb8096375aeef49bd
                   *** expected NOT to reproduce: link.exe stamps the build
                   time and the PDB GUID into the image (see windows/README.txt).
                   Compare size, exports and gate result, not the hash. ***
PE timestamp     : 6AAA367E (Tue Sep 15 23:26:06 2026 local)
PDB identity     : RSDS {0AD07C07-2F4B-48CC-A03C-C7EF14AF342B}, age 1 - the
                   value a debugger matches the .pdb against
Debug symbols    : codebrix_llama.pdb, 58,265,600 bytes, sha256
                   b14d22693507ae0708397c6265f561a2df6814b71abf3d178ce76d2654398945,
                   stored under unstripped/win-x64/ (uncompressed: under the
                   100 MB per-file limit). `dumpbin /pdbpath:verbose` on the
                   DLL with the .pdb beside it reports "PDB file found".
Dependencies     : KERNEL32.dll ADVAPI32.dll - and nothing else: no
                   VCRUNTIME140 / MSVCP140 / api-ms-win-crt-*, so no Visual C++
                   Redistributable is needed on a user's machine. (The gate's
                   allowed list was trimmed to exactly these two.)
Image flags      : Dynamic base, High Entropy VA, NX compatible (linker
                   defaults; nothing in the wrapper turns them off)
Exports          : 1,154 symbols, all llama_* / ggml_* / gguf_* /
                   codebrix_llama_* - 1,147 C functions and 5 data symbols
                   (ggml_table_f32_e8m0_half, ggml_table_f32_f16,
                   ggml_table_f32_ue4m3, ggml_table_gelu_f16,
                   ggml_table_gelu_quick_f16) listed by the generated .def,
                   plus the two identity functions; all 233 LLAMA_API
                   functions present (248/248 required incl. the extras)
Gate             : architecture ok (8664 x64), 248/248 required exports,
                   export surface exact, system-only dependencies, smoke test
                   passed (75/75 entry points, 1 device: CPU, system info
                   "SSE3 = 1 | SSSE3 = 1 | AVX = 1 | AVX2 = 1 | F16C = 1 |
                   FMA = 1 | BMI2 = 1 | LLAMAFILE = 1 | REPACK = 1"), model
                   regeneration byte-identical, conformance CPU: 12 positions,
                   64 logits each, max |diff| 4.98e-08, PASSED (the same spread
                   as linux-x64 and osx-x64: AVX2 checking an AVX2 reference)
Build noise      : five compiler warnings (C4244 conversions, C4297 in
                   llama_load_mode_from_str - all upstream code) and one CMake
                   policy warning (CMP0194, "MSVC is not an assembler for
                   language ASM", raised inside the vendored ggml tree, which
                   enables ASM; harmless, nothing is assembled). None of it is
                   ours to fix.
Reproducibility  : not measured. Only one run of the final configuration has
                   been made, and the README already expects consecutive
                   Windows links to differ (timestamp + PDB GUID).
First-run fixes  : the scripts had never run before this day. Three runs, in
                   order:
                   1. (06:16 UTC) The DLL was fine - smoke test, regeneration
                      and conformance passed - but the gate reported EVERY
                      required export missing and then "export surface exact
                      (0 symbols)". Cause: with the .pdb beside the DLL,
                      `dumpbin /exports` prints "name = name (undecorated)"
                      on every line, and the parser's pattern was anchored at
                      end-of-line; it parsed nothing. Fixed in build-common.ps1
                      (the name is the first token after the RVA), and a zero
                      parse is now itself a failure. The same run showed two
                      more things: the build-info string carried "usage: cl
                      [ option... ]" instead of the compiler version (cl prints
                      its banner on stderr and usage on stdout; taking the
                      first merged line got the wrong one - now picked by
                      content, Get-ClVersionLine); and the parsed export list,
                      once it could be read, held 952 names of which 22 were
                      C++-MANGLED ("?llama_graph_reserve@@...",
                      "?gguf_write_to_buf@@..."): upstream marks internal C++
                      functions in src/llama-ext.h and ggml/src/ggml-impl.h
                      with LLAMA_API / GGML_API, and the wrapper's global
                      GGML_SHARED/GGML_BUILD/LLAMA_SHARED/LLAMA_BUILD
                      definitions made those dllexport too. On Linux/macOS the
                      mangled names never match the llama_*/ggml_* patterns;
                      on Windows a dllexport cannot be undone by a .def. The
                      wrapper now defines none of the four macros and instead
                      GENERATES a module-definition file from the built static
                      archives (wrapper/exports-windows.cmake: every C-linkage
                      llama_*/ggml_*/gguf_* symbol defined in them, data
                      symbols marked DATA) - the same rule as
                      exports-linux.map. The three gate tools were given the
                      static CRT at the same time, so the cross-route zip runs
                      on an ARM64 machine without Visual Studio.
                   2. (06:24 UTC) Failed at the link: "unresolved external
                      symbol DATA". The generator's COMMON-symbol branch tested
                      a capture with a second MATCHES, which clobbers
                      CMAKE_MATCH_n, and wrote a line with an empty name.
                      Fixed (captures copied first).
                   3. (06:25 UTC) This build. Every check ran and passed.
Adopted into src/CodeBrix.Ollama.ModelRunner/runtimes/win-x64/native/ : YES,
                   2026-09-15, with llama.cpp's LICENSE beside it as
                   LICENSE-LlamaCpp.txt. The .pdb stored under
                   unstripped/win-x64/ (SHA256SUMS extended) in the same
                   commit. The solution builds 0/0 with the new runtimes folder
                   and the packed ModelRunner carries runtimes/win-x64/native/
                   with no doubled path.


================================================================================
win-arm64                 CROSS-BUILT, GATE INCOMPLETE, ADOPTED BY DECISION
================================================================================
  *** THIS BINARY HAS NEVER BEEN EXECUTED. Jeremy overruled the "no adoption
  without a complete gate" rule on 2026-09-15 and adopted the cross build as
  is, with the stated intention of rebuilding natively on an ARM64 machine if
  it gives trouble. Everything below the Gate line is what IS known. ***
Built            : 2026-09-16 06:26:50 UTC (2026-09-15 local)
Built by         : windows\build-win-arm64.ps1 -Route CrossFromX64 (clang-cl,
                   vcvarsall x64_arm64, target aarch64-pc-windows-msvc)
Build host       : the same Windows 11 x64 machine as win-x64 (i7-12850HX,
                   Visual Studio Professional 2026 18.10, MSVC toolset
                   14.51.36231 with the ARM64 build tools, Windows SDK
                   10.0.26100.0). NOT an ARM64 machine: this DLL has never
                   been executed anywhere.
Duration         : 34 s
Network          : not used
Compiler         : clang version 22.1.3 (Visual Studio's "C++ Clang tools for
                   Windows", VC\Tools\Llvm\x64\bin\clang-cl.exe - the x64-HOSTED
                   copy; see First-run fix), linking with link.exe from
                   VC\Tools\MSVC\14.51.36231\bin\HostX64\ARM64
cmake / ninja    : 4.4.3 / 1.13.2 (both equal to the pins)
arch options     : -DGGML_CPU_ARM_ARCH=armv8.2-a+dotprod+fp16 (reaches the
                   compiler as -march=armv8.2-a+dotprod+fp16; clang-cl accepts
                   the GNU-style flag)
extra            : -DCMAKE_C_COMPILER=<clang-cl> -DCMAKE_CXX_COMPILER=<clang-cl>
                   -DCMAKE_SYSTEM_NAME=Windows -DCMAKE_SYSTEM_PROCESSOR=ARM64
                   -DCMAKE_C_COMPILER_TARGET=aarch64-pc-windows-msvc
                   -DCMAKE_CXX_COMPILER_TARGET=aarch64-pc-windows-msvc
CPU baseline     : armv8.2-a + dotprod + fp16 (compile-time; see pins.env)
GPU backend      : none (CPU-only slice)
CRT              : static (MultiThreaded)
Patches applied  : none

File             : codebrix_llama.dll
Size             : 5,332,480 bytes
SHA256           : 82906f14e0b47d1a7fefb0c40f65905ed3f4380e5c09a3796470515699753e41
PDB identity     : RSDS {5CC5FCFE-D2A5-4BE0-4C4C-44205044422E}, age 1;
                   codebrix_llama.pdb 40,366,080 bytes, sha256
                   bbcb8c26392b0238aab248673d07fbfa3e1282f4b177f0ff912e49bbfbdf10c2,
                   stored under unstripped/win-arm64/ (SHA256SUMS extended);
                   `dumpbin /pdbpath:verbose` reports "PDB file found"
Dependencies     : KERNEL32.dll ADVAPI32.dll (system-only, static CRT took)
Image flags      : Dynamic base, High Entropy VA, NX compatible
Exports          : 1,172 symbols, all llama_* / ggml_* / gguf_* /
                   codebrix_llama_* - 1,164 C functions and 6 data symbols from
                   the generated .def plus the two identity functions; all
                   233 LLAMA_API functions present (248/248 required incl.
                   the extras). (More than x64 because the arm64 CPU kernels
                   are a different set of C functions - the same reason
                   linux-arm64 exports 1,173 to linux-x64's 1,155.)
Gate             : INCOMPLETE. The three static checks passed: architecture
                   ok (AA64 ARM64), 248/248 required exports, export surface
                   exact, system-only dependencies. The three EXECUTING checks
                   - smoke test, model regeneration, conformance - were NOT
                   RUN because an x64 host cannot execute ARM64 code, and the
                   script reports them as failures and exits 1 by design. The
                   ARM64 gate tools themselves were built (smoke-test.exe,
                   generate-test-model.exe, conformance-check.exe: all
                   AA64, all static CRT, depending only on KERNEL32 and the
                   DLL) and staged with the DLL as
                   output/staging/win-arm64/win-arm64-gate.zip (2,788,274
                   bytes) on the build machine.
Build noise      : five warnings, all deprecation (-Wdeprecated-declarations
                   for fopen/sscanf in our own gate tools, -Wexceptions in
                   upstream llama_load_mode_from_str). No CMake policy
                   warning on this route.
First-run fix    : the first cross attempt (06:26:21 UTC) stopped before
                   compiling: build-common.ps1 looked for clang-cl under
                   VC\Tools\Llvm\ARM64\bin first, which is the ARM64-HOSTED
                   binary and cannot start on an x64 machine ("not a valid
                   application for this OS platform"). Get-ClangClPath now
                   picks the host directory from the machine's own
                   architecture (x64 host -> x64 only; ARM64 host -> ARM64,
                   then x64 under emulation). The second attempt built.
Adopted into src/CodeBrix.Ollama.ModelRunner/runtimes/win-arm64/native/ : YES,
                   2026-09-15, BY JEREMY'S DECISION, WITH THE GATE INCOMPLETE
                   (the file the gate scripts would have refused). With
                   llama.cpp's LICENSE beside it as LICENSE-LlamaCpp.txt; the
                   .pdb under unstripped/win-arm64/ in the same commit. The
                   packed ModelRunner now carries all seven RIDs.
                   What this means for anyone reading later: the first real
                   run of this DLL on ARM64 Windows hardware - by a consumer,
                   or by whoever runs win-arm64-gate.zip there - IS the smoke
                   test. If it misbehaves, the expected fix is a native
                   rebuild with windows\build-win-arm64.ps1 on an ARM64
                   machine (Visual Studio with the ARM64 build tools and the
                   Clang component, cmake, ninja), which runs the whole gate
                   and replaces this entry. If it behaves, please still run
                   the three tools from win-arm64-gate.zip (windows/README.txt,
                   "FINISHING A CROSS-BUILT win-arm64") and record the result
                   here, so this slice stops being the only one never gated.


================================================================================
linux-x64                                                        BUILT + PASSED
================================================================================
Built            : 2026-09-16 05:07:04 UTC (2026-09-15 local)
Built by         : linux/build.sh x64 -> container-build.sh (container route)
Build host       : Dell Precision 7770, LMDE 7, x86_64 (native, no emulation),
                   podman 5.4.2
Derived image    : codebrix-llama-build-x86_64, built the same day from the
                   digest-pinned base (the one network step: cmake 4.4.3 and
                   ninja 1.13.2 wheels, file, xz)
Base image       : quay.io/pypa/manylinux_2_28_x86_64@sha256:a61875a2f84cab7df8de222ff12cabc08ff86eb4ad402ac90ba7bdaed9600cca
                   (AlmaLinux 8.10, container glibc 2.28)
Duration         : 24 s inside the container (the whole run, image already
                   built, about 25 s; the derived-image build before it about
                   one minute)
Network          : DISABLED (interfaces: lo) - proven by the log's first step
Compiler         : cc (GCC) 14.2.1 20250110 (Red Hat 14.2.1-11), GNU ld 2.41
cmake / ninja    : 4.4.3 / 1.13.2 (both equal to the pins)
arch options     : (none) - the x64 baseline is what GGML_NATIVE=OFF gives
CPU baseline     : x86-64 with AVX2, FMA, F16C, BMI2 (no AVX-512)
GPU backend      : none (CPU-only slice)
Patches applied  : none

File             : libcodebrix_llama.so
Build-id         : fd7e8b30909507f14bf523c03c402c40e1ae4f98
Size (stripped)  : 6,138,928 bytes
Size (unstripped): 114,326,288 bytes (full DWARF from -g; see unstripped/)
Size (xz staged) : 1,801,368 bytes
SHA256           : a2c7572063b149dea0371b8767aa23554df305761f6373423ae55a92334ac8d8
SHA256 unstripped: 40c32e86227401824230eccd5089b2da468f3791b9ec7c33db6610fd2b061b9f
glibc floor      : 2.27 (allowed <= 2.28; loads on Debian 10+, Ubuntu 20.04+,
                   RHEL 8+)
Dependencies     : ld-linux-x86-64.so.2 libc.so.6 libdl.so.2 libm.so.6
                   libpthread.so.0 - system-only; no libstdc++/libgcc_s
                   (linked statically)
Exports          : 1,155 symbols, all llama_* / ggml_* / gguf_* /
                   codebrix_llama_*; all 233 LLAMA_API functions present
                   (248/248 required incl. the extras)
Gate             : architecture ok, 248/248 required exports, export surface
                   exact, ldd -r clean, system-only dependencies, glibc floor
                   ok, smoke test passed (75/75 entry points, 1 device: CPU),
                   model regeneration byte-identical, conformance CPU:
                   12 positions, 64 logits each, max |diff| 4.98e-08, PASSED
                   (the same spread the macOS slices show)
Reproducibility  : measured with a second from-scratch run 28 minutes later
                   (build-id d9da1cd07204b0ad17af095861cf4f93139379db, sha256
                   69ee876d...): the stripped files differ in exactly 24 bytes
                   and the unstripped files in the same 24 - the 20-byte GNU
                   build-id note plus the four digits of the build time that
                   container-build.sh bakes into codebrix_llama_build_info().
                   All code and data identical; the repeat run passed the whole
                   gate with the identical conformance spread. So: reproducible
                   except for the embedded timestamp and the build-id derived
                   from it - the ELF counterpart of the macOS LC_UUID finding.
                   Compare size, exports and gate result, not the hash. The
                   arm64 and riscv64 builds carry the same timestamp and
                   build-id and are expected to behave the same way; not
                   re-measured (each emulated run is 5-7 minutes).
First-run fix    : the first linux-x64 run (2026-09-16 05:05 UTC) built the
                   library but the gate tools could not link against it: the
                   wrapper's Linux link options carried -Wl,--exclude-libs,ALL
                   as "belt and braces" with the version script, and on ELF
                   that option makes every symbol from a static archive HIDDEN,
                   which a version script's global: list cannot undo - the .so
                   exported only the two codebrix_llama_* functions. The flag
                   was removed from wrapper/CMakeLists.txt (the version script
                   alone gives the exact surface) and the second run passed the
                   whole gate. macOS was never affected (it uses
                   -exported_symbols_list, no such flag).
Adopted into runtimes/linux-x64/native/ : YES, 2026-09-15, with
                   LICENSE-LlamaCpp.txt; unstripped twin stored xz-compressed
                   (xz -9e) as unstripped/linux-x64/libcodebrix_llama.so.xz,
                   17,829,980 bytes - the "SHA256 unstripped" above is the
                   decompressed file's hash (build-id verified equal before and
                   after the round trip through xz)


================================================================================
linux-arm64                                                      BUILT + PASSED
================================================================================
Built            : 2026-09-16 05:12:46 UTC (2026-09-15 local)
Built by         : linux/build.sh arm64 -> container-build.sh (container route)
Build host       : the same x86_64 laptop (Dell Precision 7770, LMDE 7), under
                   qemu-user emulation (qemu-user-static 10.0.13, binfmt),
                   podman 5.4.2 - the dav1d route, not a native arm64 machine
Derived image    : codebrix-llama-build-aarch64, built the same day from the
                   digest-pinned base (the one network step)
Base image       : quay.io/pypa/manylinux_2_28_aarch64@sha256:162c81dfd3efc710732a571717d3c916a6945ebf279e879ddee3243af96fe46f
                   (AlmaLinux 8.10, container glibc 2.28)
Duration         : 334 s inside the container (emulated; 24 jobs)
Network          : DISABLED (interfaces: lo)
Compiler         : cc (GCC) 14.2.1 20250110 (Red Hat 14.2.1-11), GNU ld 2.41
cmake / ninja    : 4.4.3 / 1.13.2 (both equal to the pins)
arch options     : -DGGML_CPU_ARM_ARCH=armv8.2-a+dotprod+fp16
CPU baseline     : armv8.2-a with dotprod and fp16 (Cortex-A75 / Neoverse-N1 /
                   Raspberry Pi 5 and later; excludes Raspberry Pi 4)
GPU backend      : none (CPU-only slice)
Patches applied  : none

File             : libcodebrix_llama.so
Build-id         : ed211a6aa0b77d2c5d13bf07eed093d6b448de27
Size (stripped)  : 5,585,600 bytes
Size (unstripped): 112,111,304 bytes (full DWARF from -g; see unstripped/)
Size (xz staged) : 1,527,656 bytes
SHA256           : 037dbe5182dd85b5266f84f2aadea09fcadfabe8509d5f57deed991f33ae3a51
SHA256 unstripped: 321c65e12ae1086b4de5da427af1d5ee9ede81ed96b73d0577a406d734f76d25
glibc floor      : 2.27 (allowed <= 2.28)
Dependencies     : ld-linux-aarch64.so.1 libc.so.6 libdl.so.2 libm.so.6
                   libpthread.so.0 - system-only; no libstdc++/libgcc_s
Exports          : 1,173 symbols, all llama_* / ggml_* / gguf_* /
                   codebrix_llama_*; all 233 LLAMA_API functions present
                   (248/248 required incl. the extras)
Gate             : architecture ok, 248/248 required exports, export surface
                   exact, ldd -r clean, system-only dependencies, glibc floor
                   ok, smoke test passed UNDER EMULATION (75/75 entry points,
                   1 device: CPU, system info "NEON = 1 | ARM_FMA = 1 |
                   FP16_VA = 1 | DOTPROD = 1 | LLAMAFILE = 1 | REPACK = 1"),
                   model regeneration byte-identical, conformance CPU:
                   12 positions, 64 logits each, max |diff| 1.43e-04, PASSED.
                   That spread is IDENTICAL to the one the native Apple
                   Silicon build recorded for its CPU path (osx-arm64 entry
                   above), so it is the fp16 vector arithmetic of the arm64
                   kernels, not an emulation artefact; x64 sits at 5e-08
                   because its kernels stay in f32.
Note             : the whole gate ran under qemu-user, which exercises the
                   NEON/dotprod/fp16 code paths but is not a reference for
                   speed. Nothing has yet run this file on real arm64
                   hardware; the first real-hardware run should be recorded
                   here.
First-run fix    : the first arm64 attempt (05:08 UTC) failed while BUILDING
                   THE DERIVED IMAGE, in Containerfile.aarch64's own toolchain
                   probe: the probe program expected vdotq_s32 over all-ones
                   bytes to put 16 in each lane, but the instruction sums four
                   1*1 products per lane and puts 4, so the probe exited -12
                   (podman reported exit 244). The intrinsics compiled and ran
                   correctly; only the expected value was wrong. Fixed in the
                   Containerfile (expect 4) and the second run passed.
Adopted into runtimes/linux-arm64/native/ : YES, 2026-09-15, with
                   LICENSE-LlamaCpp.txt; unstripped twin stored xz-compressed
                   (xz -9e) as unstripped/linux-arm64/libcodebrix_llama.so.xz,
                   16,999,208 bytes - the "SHA256 unstripped" above is the
                   decompressed file's hash (build-id verified equal before and
                   after the round trip through xz)


================================================================================
linux-riscv64                                                    BUILT + PASSED
================================================================================
Built            : 2026-09-16 05:28:47 UTC (2026-09-15 local)
Built by         : linux/build.sh riscv64 -> container-build.sh (container route)
Build host       : the same x86_64 laptop (Dell Precision 7770, LMDE 7), under
                   qemu-user emulation (qemu-user-static 10.0.13, binfmt),
                   podman 5.4.2 - the dav1d route, not a native riscv64 board
Derived image    : codebrix-llama-build-riscv64, rebuilt the same day from the
                   digest-pinned base (the one network step) - see First-run fix
Base image       : quay.io/pypa/manylinux_2_39_riscv64@sha256:04b3acc69a6cebb10ac1252587d202acce6445d91c783d3f92229bb2b2483069
                   (Rocky Linux 10.2, container glibc 2.39; the system gcc, not
                   a gcc-toolset)
Duration         : 311 s inside the container (emulated; 24 jobs)
Network          : DISABLED (interfaces: lo)
Compiler         : cc (GCC) 14.3.1 20251022 (Red Hat 14.3.1-4)
cmake / ninja    : 4.4.3 / 1.13.2 (both equal to the pins; riscv64 wheels
                   exist on PyPI, nothing was built from source)
arch options     : -DGGML_RVV=OFF -DGGML_RV_ZFH=OFF -DGGML_RV_ZVFH=OFF
                   -DGGML_RV_ZICBOP=OFF -DGGML_RV_ZIHINTPAUSE=OFF
CPU baseline     : rv64gc, scalar - no vector extension (a board without RVV
                   would otherwise crash; see pins.env)
GPU backend      : none (CPU-only slice)
Patches applied  : none

File             : libcodebrix_llama.so
Build-id         : a85df2912e3d3a1c44d86cdb550fb6b38348eb8e
Size (stripped)  : 4,676,056 bytes
Size (unstripped): 79,725,640 bytes (full DWARF from -g; see unstripped/)
Size (xz staged) : 1,676,008 bytes
SHA256           : 06315b609bc2272a9bcd6d08364bd73997eabc320d02209235e929396dd1e5e4
SHA256 unstripped: 2ff7bf71acbd92d2aba2263bdaea225b37c213c123e0b3e0d5e49b0063f30123
glibc floor      : 2.38 (allowed <= 2.39; Debian 13 / Ubuntu 24.04 and newer)
Dependencies     : ld-linux-riscv64-lp64d.so.1 libc.so.6 libm.so.6 - system-only
                   (glibc 2.34+ folds libpthread and libdl into libc, so the
                   shorter list is correct); no libstdc++/libgcc_s
Exports          : 1,146 symbols, all llama_* / ggml_* / gguf_* /
                   codebrix_llama_*; all 233 LLAMA_API functions present
                   (248/248 required incl. the extras)
Gate             : architecture ok (UCB RISC-V, RVC, double-float ABI),
                   248/248 required exports, export surface exact, ldd -r
                   clean, system-only dependencies, glibc floor ok, smoke test
                   passed UNDER EMULATION (75/75 entry points, 1 device: CPU,
                   system info "LLAMAFILE = 1 | REPACK = 1" - no vector
                   features, as pinned), model regeneration byte-identical,
                   conformance CPU: 12 positions, 64 logits each, max |diff|
                   1.32e-07, PASSED (scalar f32, the same order as x64).
Note             : the whole gate ran under qemu-user. Nothing has yet run this
                   file on real riscv64 hardware; the first real-hardware run
                   should be recorded here.
First-run fix    : the first riscv64 attempt (05:19 UTC) compiled every object
                   and failed at the final link: "cannot find -lstdc++". The
                   x86_64 and aarch64 images are AlmaLinux 8 with gcc-toolset-14,
                   whose libstdc++-devel ships libstdc++.a; this image is Rocky
                   Linux 10 with the system gcc, where the static libstdc++ is
                   the separate package libstdc++-static (14.3.1-4.4.el10, in
                   the enabled CRB repository). Containerfile.riscv64 now
                   installs it beside file and xz and asserts the archive is
                   present; the image was rebuilt (FORCE_IMAGE_REBUILD=1) and
                   the second run passed the whole gate. The static-runtime
                   design is unchanged: no libstdc++.so.6 / libgcc_s.so.1
                   dependency, exactly as on the other two slices.
Adopted into runtimes/linux-riscv64/native/ : YES, 2026-09-15, with
                   LICENSE-LlamaCpp.txt; unstripped twin stored xz-compressed
                   (xz -9e) as unstripped/linux-riscv64/libcodebrix_llama.so.xz,
                   14,022,316 bytes - the "SHA256 unstripped" above is the
                   decompressed file's hash (build-id verified equal before and
                   after the round trip through xz)
================================================================================
