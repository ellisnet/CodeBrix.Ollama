<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
  </PropertyGroup>

  <ItemGroup>
    <!-- The DI registration extension method lives in this library, so it needs the abstractions -->
    <PackageReference Include="Microsoft.Extensions.DependencyInjection.Abstractions" Version="10.0.12" />
  </ItemGroup>

  <!-- CodeBrix.Ollama.ModelManager - the whole reason this library exists, and the ONLY project in the
       sample that references it. A project reference for now; it becomes a package reference to
       CodeBrix.Ollama.ModelManager.MitLicenseForever later, and nothing else has to change: this
       library uses the public surface only, exactly as a consumer of the package does. -->
  <ItemGroup>
    <ProjectReference Include="..\..\..\..\..\src\CodeBrix.Ollama.ModelManager\CodeBrix.Ollama.ModelManager.csproj" />
  </ItemGroup>
</Project>
