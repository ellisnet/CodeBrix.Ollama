<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
  </PropertyGroup>

  <!-- Deliberately no references at all: this library is the UI-free half of the chat terminal.
       It turns keystrokes into edited lines and text into terminal output, and knows nothing
       about the terminal control, the platform, or the two model libraries. -->
</Project>
